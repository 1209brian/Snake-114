# 貪食蛇遊戲 (Snake Game) 技術規格書

本文件詳細說明了基於 HTML5、CSS3 與原生 JavaScript 開發的貪食蛇遊戲邏輯與結構。

## 1. 專案結構
* `index.html`: 定義遊戲畫面的 DOM 結構，包含遊戲畫板 (Board) 與控制介面。
* `style.css`: 處理網格佈局、顏色主題與蛇/食物的視覺樣式。
* `main.js`: 核心邏輯實作，包含狀態管理、碰撞偵測與遊戲循環。

---

## 2. 資料結構與狀態管理
遊戲透過以下全域變數維護即時狀態：

| 變數名稱 | 資料類型 | 功能描述 |
| :--- | :--- | :--- |
| `snake` | `Array<{x, y}>` | 儲存蛇身座標的陣列，`index 0` 為蛇頭。 |
| `direction` | `{x, y}` | 當前物理移動方向向量。 |
| `nextDirection` | `{x, y}` | 緩存下一幀的方向，防止 180 度自殺式轉向。 |
| `food` | `{x, y}` | 當前食物在網格中的座標位置。 |
| `score` | `Number` | 玩家目前得分，隨吃掉食物的數量增加。 |
| `speed` | `Number` | 遊戲循環間隔（毫秒），隨分數增加而縮短（增加難度）。 |

---

## 3. 地圖繪製邏輯
* **網格定義**：使用 `GRID_SIZE = 30` 定義地圖為 30x30 的正方形網格。
* **動態生成**：`initBoard()` 函式會生成 900 個 `div.cell` 並插入 `#gameBoard`。
* **座標定位**：每個格子透過 `data-x` 與 `data-y` 屬性標記，便於透過 `querySelector` 進行精準渲染。
* **渲染流程**：
    1.  `clearBoard()`：移除所有格子的 `.snake` 與 `.food` 樣式。
    2.  `drawFood()`：將 `food` 座標對應的格子加上 `.food` 樣式。
    3.  `drawSnake()`：遍歷 `snake` 陣列，將對應格子加上 `.snake` 樣式。

---

## 4. 遊戲核心機制

### A. 移動與轉向
* **方向控制**：監聽鍵盤事件（ArrowUp, ArrowDown, ArrowLeft, ArrowRight）。
* **轉向限制**：若玩家嘗試轉向與目前相反的方向（例如向左移動時按向右），該輸入會被忽略。
* **邊緣傳送**：`wrapPosition()` 函式實作了穿牆機制。當蛇頭座標溢出邊界時，會自動轉向對向邊界。

### B. 食物與成長
* **隨機生成**：食物會隨機出現在地圖上的空位。
* **判定與獎勵**：
    1.  當蛇頭座標等於食物座標時，`score` 加 1。
    2.  蛇身長度增加（不執行 `pop()` 操作）。
    3.  `speed` 減少 2ms，加快遊戲節奏，最小限度為 10ms。

### C. 死亡判定
* **自我碰撞**：透過 `isCollision()` 檢查蛇頭是否與 `snake` 陣列中現有的節點座標重疊。
* **結束處理**：若碰撞發生，停止 `setInterval` 循環，並彈出 `alert('Game Over')`。

---

## 5. 控制介面功能
* **開始遊戲 (`Start`)**：調用 `startGame()`，重置所有遊戲變數並初始化網格。
* **暫停遊戲 (`Pause`)**：調用 `togglePause()`，清除或重新設定計時器以達到暫停/恢復效果。
* **分數看板**：即時將 `score` 變數內容反映在介面 UI 上。

---

## 6. CSS 變數與樣式
專案使用 `:root` 定義顏色主題，方便統一修改視覺風格：
* 蛇身：`--primary` (預設白色)。
* 食物：`--accent` (預設紅色)。
* 背景：`--board-bg` (預設深灰色)。